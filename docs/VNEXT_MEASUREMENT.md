# VNEXT_MEASUREMENT — 测量层 vNext 预注册(今日密封,07-19 后执行)

> **密封日期:2026-07-10(pre-data:首批到期卡记分于当晚 22:30 之后,密封时零真实记分样本)。**
> 本文为纸面预注册,当日不改任何在线代码/指标/展示;执行时点=**07-19(两份周报
> 07-12/07-19 落地)之后**,与解冻捆绑(见 c)同批走正式变更流程(PROCESS §1-§4)。
> 口径纪律同 BRIER_SPEC:落档后**改代码就文档,不许反向**;与本文冲突的实现视为 bug。

## 密封头部(状态锚)

| 项 | 值 |
|---|---|
| 当前 commit(编排节点 HEAD) | `c4ffe8ba3548ccd4b90ca3d355c71dd4cc105f63` |
| 工作树状态 | 除本行动单三处产物(本文件、`docs/audit/20260710_adj_dual_check/`、`docs/audit/20260706_subjective_prob/06_acceptance.md` 第2项回填)外干净;**零 src/ 改动** |
| 实际部署 commit(数据节点) | `f7b3bae`(DEPLOY_STATE.md 2026-07-10 10:51:14 UTC+8,dirty 0;其后 840310c/c4ffe8b 为编排节点侧脚本与文档,不涉数据节点运行面) |
| 现行 BRIER_SPEC 版本标识 | `docs/BRIER_SPEC.md` 落档 2026-07-06(commit `6f69b4a`,DECISIONS #41);版本纪元 prompt_hash B6 `8528ca795ca4c6b8` / B8 `780916554dc9be8b` |

## a. Brier 主指标【今日密封,pre-data】

- **主指标 = 无条件 Brier**:样本域为全部带 subjective_prob 的到期卡
  (verdict ∈ {对,错,平}),outcome 定义 **对=1,错=0,平=0**。
  依据 = 发卡 prompt 原文对 prob 的**无条件**定义("带内不算兑现",措辞明示)——
  模型自报的是无条件兑现概率,主指标必须与被测量对象的语义同构。
- **条件 Brier(现行 BRIER_SPEC 第1条剔平口径)降为辅指标**,保留报告;
  读辅指标必须随附 `1/(1−flat_rate)` 去偏系数附注(BRIER_SPEC 附注同款),不得直读。
- **vNext 无条件 Brier 为重新派生的并列历史序列**:从 card_score/decision_score ×
  卡表 prob 全量重算(分数是行情与卡的确定性函数,可重算),与既有序列并列展示;
  **不改写既有周报(b7_weekly)任何原记录**——旧记录=旧口径留痕,append 新键不动旧键。
- BRIER_SPEC 其余裁决(固定分桶/卡型分层/版本分组/复算规则/禁改 prompt 引导措辞)
  原样继承,不因主辅互换而松动。

## b. headline 三件套(主读数重排)

成绩单 headline 主读数改为三件套(均含节点/个股两层):

1. **方向卡命中率(Wilson 95%)**:仅 direction ∈ {偏多,偏空} 的到期卡,
   对/(对+错),带 Wilson 区间(点估计禁裸报,现行 `_stats` 纪律不变);
2. **覆盖率**:方向卡中判出对错的占比 = (对+错)/(对+错+平)——"开口说话时说得多干脆";
3. **中性率**:中性卡占全部到期卡的比例——"多久弃权一次"。

**混合累计命中率(现行 `stats.cum.hit_rate`,方向卡与中性卡混桶)降为次要行**:
中性卡走 ±2pp 带、方向卡走 ±1pp 阈,判定难度不对称,混桶数系统性虚高,
不得再作 headline。字段不删(向后兼容),仅展示降位。

> 过渡期读法约定(07-12/07-19 两份周报,不改代码):混合累计命中率按已知虚高读,
> 实际参考 `stats.by_direction`。

## c. 并入解冻捆绑(共四件)

本 vNext 执行时与下列解冻项同批(同一变更窗口、同一 DECISIONS 记录、
prompt 涉及项换哈希重登版本):

1. PIT 结构护栏(含研报查询补上界);
2. 资金信号标准化;
3. 候选门槛 n_active→n_agree;
4. decision+evidence 两链 SYSTEM 补"数字与单位原样照抄,禁自行换算"铁律
   (structure.py/report.py 已有,两判断链因冻结期硬闸延至此批)。

〔原列"prompt 条件措辞复议"已删除:与 a 冲突——本次 vNext 保持 prompt 无条件表述;
未来若改为条件概率自报 = 另开版本决策,换哈希另立纪元,不在本捆绑内。〕

## d. 分类依据(仅限本单)

本单所允许的"今日动作"仅限于**不替代、不重排、不改写既有正式指标的独立观察产物**
(复权双口径只读核查归档、验收回填、本预注册文档、未合并的分支备份),
不构成此次冻结期的策略/测量变更;其余展示或指标改动一律按破闸处理。
**本条不升格为全局规则**,是否推广另由正式决策记录裁定。

## 执行清单(07-19 后,勿提前)

- [ ] 合并分支 `vnext-measurement-20260710`(a/b 的 diff+单测已备,见
      `docs/audit/20260710_adj_dual_check/` 取证包;合并前 rebase 复验)
- [ ] c 四件同批实施,prompt 涉及项重登 prompt_hash 并更新 `_PROMPT_LABELS`
- [ ] 周报模板/前端展示按 b 重排 headline
- [ ] DECISIONS 记录 + B7_SCORECARD/BRIER_SPEC 文档同步(主辅互换须回写 SPEC)
