| 48 | 2026-08-24 | **DeepSeek 降本第二批:B1 批量化 + LLM 夜间窗口**(展示/排程层,零口径改动)——①**B1 逐条→整批**(默认 8 条/次,`B1_BATCH` 可调):#47 里留档「有批内串扰风险、单独排期」的那条本批落地,护栏=规则块前置(判断口径逐字不变,同时成为平台前缀缓存的公共前缀)+ 每条带序号 `### i` + **回写严格按 i 对齐,越界/重复/缺 i/条数不足一律丢弃该条**(llm_done 保持 false 下档重跑,绝不按数组位置猜——错位回写是事实层幻觉,踩红线)+ 整批失败自动拆成逐条(某条正文有毒不会卡死队列,最坏退回旧行为);②**LLM 夜间窗口 `config.llm_allowed`:只在 UTC+8 18:00–08:00 调 DeepSeek**,白天留 09:45/15:15 两档补做点(对齐 cron 火点,命中给 15min 宽限抵消 flock 排队);白天静默档 B1/盘中增量整步跳过,热点/研报观点降级为「统计行最新+沿用当天上一版叙述」且**不落指纹**(落了会把白天旧叙述钉到夜间);③盘前 run_premarket **08:30→07:40** 挪进窗口内(美股夏令时 04:00 收盘,隔夜数据早已就绪,零内容损失);④`llm.chat_json` 加窗口外**告警日志但不硬拦**——硬拦会把手动补跑变成一片假失败,比它防的问题更糟(告警载体教训) | 使用者 08-24 指令「看下能否再减少 DeepSeek 调用,并且调用时间改为 UTC+8 晚 18 点到早 8 点」。白天档去留经使用者当场拍板:选「白天留 2 个补做点」(而非彻底静默)、盘前「提前到 07:40」。盘点(08-24 实测):A股 B1 逐条 **348 次/日**是绝对大头(#47 未触碰),hotspots+digest 约 87 次(指纹命中率仅 22%),report_increment 约 10 次。**判断链零触碰**:B1–B8 prompt 规则文字逐字未改、口径/权重/发卡/记分全不动,#41 硬闸不适用(展示/排程层降本,先例 #44/#45/#47)。代价如实记:白天新增新闻的摘要/情绪最长滞后到下一个补做点,热点归因白天不刷新(热度数字仍每档最新)——使用者已知情选定。实测:真 DeepSeek 8 条批量一次调用 8/8 全对齐、零串扰、「62204%」未被换算成「6万倍」(百分比≠倍护栏在批量下仍生效);tests 新增 29 条(批内错位五种情形逐一钉死+窗口边界14例+白天降级不落指纹),全库 **90 passed**;取证 docs/audit/20260824_llm_night_window/ |
