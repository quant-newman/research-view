=== 生产 A/B:门控命中路径实测(数据节点,2026-08-21 UTC+8) ===

【12:45:31】首次复跑(此时 12:45 档 cron 的 run_light 正在采研报,digest 输入真在变)
  热点综述:输入指纹未变,复用上一版(省 1 次 LLM)
hotspots.persist -> 10
  用时 0.1s
research_digest.persist -> {'changes': 0, 'views': 15}
  用时 14.9s          ← 未命中。原因=同档 cron 刚采进新研报,标题块变了,属设计内重算

【12:57:12】远端确认 IDLE 后的干净 A/B(连跑两轮 + 指纹稳定性)
  研报观点提炼:输入指纹未变,复用上一版(省 1 次 LLM)
  digest 第1次: 0.0s
  热点综述:输入指纹未变,复用上一版(省 1 次 LLM)
  hotspot 第1次: 0.1s
  研报观点提炼:输入指纹未变,复用上一版(省 1 次 LLM)
  digest 第2次: 0.0s
  热点综述:输入指纹未变,复用上一版(省 1 次 LLM)
  hotspot 第2次: 0.1s
  digest 指纹两算: 5df6ff8f520a 5df6ff8f520a
  hotspot 指纹两算: 8b4a53c68409 8b4a53c68409

结论:
- 两处门控在生产真实数据上均命中,耗时 0.0-0.1s(未命中时 digest 约 15s、hotspot 数十秒),零 LLM;
- 同一输入连算两次指纹同值 → 排序/取数不确定性不会让门控"永不命中"(这是本设计最大的失效模式);
- 未命中发生在输入真变时,方向正确。

【排查留痕】过程中一次 `ssh "pgrep -f run_light"` 恒报 BUSY,系 pgrep -f 匹配到自己的
bash -c 命令行(自匹配),非远端真忙;改用 pgrep -f "[r]un_light.py" 后正常。
